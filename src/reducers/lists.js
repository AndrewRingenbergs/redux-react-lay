export default function() {
  return {
      todoList: [1, 2, 3, 4, 5]
    }
}

/*export default function basic(state = [], action) {
  switch (action.type) {
    case 'NEW':
      return [2, 3];
    default:
      return state;
  }
}*/